{
    "name": "bravepickle/rozetka-checkout-test",
    "description": "PoC test task to demonstrate high load processing clients checkout",
    "type": "project",
    "license": "Apache 2.0",
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "authors": [
        {
            "name": "Bravepickle"
        }
    ],
    "minimum-stability": "stable",
    "require": {
        "ext-redis": "*",
        "php": "^8.3"
    },
    "scripts": {
        "app:combine": ["@php bin/combine"]
    }
}
